apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: default
spec:
  selector:
    app: redis
  type: NodePort
  ports:
    - name: tcp-port
      port: 6379
      targetPort: 6379
      protocol: TCP
