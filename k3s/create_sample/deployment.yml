
apiVersion: apps/v1
kind: Deployment
metadata:
  name: create-sample-runner
  labels:
    app: create-sample-runner
spec:
  replicas: 1
  selector:
    matchLabels:
      app: create-sample-runner
  template:
    metadata:
      labels:
        app: create-sample-runner
    spec:
      containers:
        - name: create-sample-runner
          image: virtool/create_sample
          imagePullPolicy: IfNotPresent
          command:
            [workflow, run-from-redis, jobs_create_sample]
          env:
            - name: VT_RUN_FROM_REDIS_DEV_MODE
              value: 'true'
            - name: VT_RUN_FROM_REDIS_IS_ANALYSIS_WORKFLOW
              value: 'true'
            - name: VT_RUN_FROM_REDIS_JOBS_API_URL
              value: 'http://jobs-api.default.svc.cluster.local:9990/api'
            - name: VT_RUN_FROM_REDIS_REDIS_URL
              value: 'redis://redis.default.svc.cluster.local:6379'
